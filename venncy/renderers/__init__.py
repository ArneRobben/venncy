# Renderer convenience wrappers for venncy.
#
# Each sub-module (matplotlib, plotly, pyvis) imports its rendering
# library lazily so that users only need to install the backends they
# actually use.
